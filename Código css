/* Reset básico */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: Arial, sans-serif;
    line-height: 1.6;
    background-color: #000; /* Fundo preto para o corpo */
    color: #fff; /* Texto branco por padrão */
}

/* Container centralizado */
.container {
    width: 90%;
    max-width: 1200px;
    margin: 0 auto;
}

/* Estilo do cabeçalho */
header {
    background-color: #111; /* Fundo escuro para o cabeçalho */
    color: #fff; /* Texto branco no cabeçalho */
    padding: 1rem 0;
}

header h1 {
    font-size: 2rem;
    margin-bottom: 0.5rem;
}

nav ul {
    list-style: none;
    display: flex;
    justify-content: center;
}

nav ul li {
    margin: 0 1rem;
}

nav ul li a {
    color: #d953c6; /* Cor roxa para links no cabeçalho */
    text-decoration: none;
    font-weight: bold;
}

nav ul li a:hover {
    text-decoration: underline;
}

/* Estilo da seção hero */
.hero {
    background: url('hero-bg.jpg') no-repeat center center/cover;
    color: #fff; /* Texto branco na seção hero */
    padding: 4rem 0;
    text-align: center;
}

.hero h2 {
    font-size: 3rem;
    margin-bottom: 0.5rem;
}

.hero p {
    font-size: 1.2rem;
    margin-bottom: 1rem;
}

.hero button {
    padding: 0.8rem 2rem;
    font-size: 1rem;
    border: none;
    color: #fff;
    background-color: #d953c6; /* Cor roxa para o botão */
    cursor: pointer;
    border-radius: 5px;
}

.hero button:hover {
    background-color: #b74b9b; /* Cor roxa escura para hover */
}

/* Estilo do conteúdo principal */
.content {
    padding: 2rem 0;
}

.content h2 {
    font-size: 2rem;
    margin-bottom: 1rem;
}

.card-container {
    display: flex;
    overflow-x: auto; /* Corrige a rolagem horizontal */
    gap: 1rem;
}

.card {
    flex: 0 0 auto;
    width: 200px; /* Largura dos cards */
    height: 300px; /* Altura dos cards */
    overflow: hidden;
    border-radius: 10px;
    text-decoration: none;
    position: relative;
}

.card img {
    width: 100%;
    height: 100%; /* Ajusta a altura das imagens para o card */
    object-fit: cover; /* Faz a imagem cobrir o card sem distorcer */
    display: block;
    border-radius: 10px;
}

/* Estilo do rodapé */
footer {
    background-color: #111; /* Fundo escuro para o rodapé */
    color: #fff; /* Texto branco no rodapé */
    padding: 1rem 0;
    text-align: center;
}

footer p {
    font-size: 0.8rem;
}
